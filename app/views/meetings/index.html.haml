%h1 Listing meetings

%table
  %tr
    %th Meeting time
    %th Title
    %th Description
    %th Place
    %th Tutor
    %th Total places
    %th
    %th
    %th

  - @meetings.each do |meeting|
    %tr
      %td #{meeting.start_day}  #{meeting.start_time} to #{meeting.end_time}
      %td= meeting.title
      %td= meeting.description
      %td= meeting.place
      %td= meeting.tutor
      %td= meeting.total_places
      %td= link_to 'Show', meeting
      %td= link_to 'Edit', edit_meeting_path(meeting)
      %td= link_to 'Destroy', meeting, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Meeting', new_meeting_path
